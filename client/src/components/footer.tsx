import React from "react";

function Footer() {

    // This will be worked on later, as I do want the app to function before deciding what the Footer should look like
    return (
        <div></div>
    )
}